/** @type {import('next').NextConfig} */

import { join } from 'path'
const nextConfig = {
    output:'export',
}
// export const sassOptios = {
//     includepaths: [join(--dirname, 'style')],
// }

export default nextConfig
